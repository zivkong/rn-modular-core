const providersInit = () => {
  // Initiate providers during AFTER navigation and redux is mounted
}

const providersCleanup = () => {
  // Cleanup providers when user restart the app
}

export { providersInit, providersCleanup }
